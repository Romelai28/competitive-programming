cout << setprecision(numeric_limits<long double>::digits10 + 1);
// Funcionan con int y float.
string stri = to_string(myInt); int myint1 = stoi(myString);
// lower_bound: find the first pos in which val could be inserted without changing the order.
// upper_bound: find last postion in which val could be inserted without changing the order.
lower_bound(v.begin(), v.end(), 30);
*max_element(a.begin(), a.end()); *min_element(a.begin(), a.end());